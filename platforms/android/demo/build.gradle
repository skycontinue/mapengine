apply plugin: 'com.android.application'

android {
  compileSdkVersion 30

  def apiKey = project.hasProperty('nextzenApiKey') ? nextzenApiKey : System.getenv('NEXTZEN_API_KEY')

  defaultConfig {
    minSdkVersion 16
    targetSdkVersion 30
    buildConfigField 'String', 'NEXTZEN_API_KEY', '"yuLFXt1dQgiRNmlS3Xvimg"'
  }

  sourceSets.main {
    assets.srcDirs = ['../../../scenes']
  }

  buildTypes {
    debug {
      debuggable true
      jniDebuggable true
    }
    release {
      minifyEnabled true
    }
  }

  androidResources {
    ignoreAssetsPattern '!*.ttf'
  }
  ndkVersion '21.4.7075529'

}

dependencies {
  implementation 'androidx.appcompat:appcompat:1.2.0'
  implementation 'androidx.annotation:annotation'
  implementation 'com.squareup.okio:okio:2.1.0'
  implementation project(path: ':tangram')
}
